{
  "bsonType": "object",
  "required": ["user_id", "email", "name"],
  "properties": {
    "user_id": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "email": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "name": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "preferences": {
      "bsonType": "object",
      "properties": {
        "wake_up_window_start": {
          "bsonType": "date",
          "description": "must be a date"
        },
        "wake_up_window_end": {
          "bsonType": "date",
          "description": "must be a date"
        }
      }
    }
  }
}
